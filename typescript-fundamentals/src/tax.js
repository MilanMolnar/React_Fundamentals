export function calculateTax(income) {
  return income * 0.3;
}

/**
 * Calculate income tax
 * @param {number} income - Net salary after expenses
 * @returns {number}
 */
export function jsDocTypeCheckedTaxCalculator(income) {
  return income * 0.3;
}
